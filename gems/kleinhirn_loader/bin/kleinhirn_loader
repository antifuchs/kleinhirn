#!/usr/bin/env ruby
# typed: false
# frozen_string_literal: true

require 'kleinhirn_loader'

raise "USAGE: #{$PROGRAM_NAME} entrypoint_file.rb entrypoint_expression" if ARGV.length != 3

kh = KleinhirnLoader.new(ARGV[2])
kh.load_entrypoint(ARGV[1])
kh.repl
